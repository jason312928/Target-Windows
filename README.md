# Target-Windows
Native Windows client for Target

The user-mode sing-box runtime uses `%LOCALAPPDATA%\Target\sing-box`. Install the
pinned Windows x64 engine with `Scripts\install_sing_box.ps1`; the script verifies
the SHA-256 digest published on the official SagerNet GitHub release before install.
